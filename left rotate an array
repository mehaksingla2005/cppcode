#include <bits/stdc++.h> 
vector<int> rotateArray(vector<int>& arr, int n) {
    // Write your code here.
    int i=0;
    int temp=arr[i];
    for(int j=0;j<n-1;j++){
         arr[j]=arr[j+1];
    }
    arr[n-1]=temp;
    return arr;
}
